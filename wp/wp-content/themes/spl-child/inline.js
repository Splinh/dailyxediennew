/* SPL Child — inline.js placeholder */
/* Add quick JS fixes here. File is only enqueued when size > 300 bytes. */
/* --------------------------------------------------------------------- */
/* Example:
document.addEventListener('DOMContentLoaded', function() {
    console.log('SPL Child loaded');
});
*/
